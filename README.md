# LED-Blinking-using-FPGA
The LED blinking implementation helped in understanding synchronous digital design concepts such as clocking, counters, and reset mechanisms. It also demonstrated how output pins on the FPGA can be mapped to physical LEDs using constraint files. Successful observation of the blinking LEDs confirmed that the design was correctly synthesized.
